using FlaxEngine;
using System.Collections.Generic;

namespace BehaviourTree
{
    /*public partial class Blackboard
    {
        private Dictionary<string, Actor> blackboardActorData = new Dictionary<string, Actor>();

        public void SetValue(string key, Actor value)
        {
            blackboardActorData[key] = value;
        }

        public bool GetValue(string key, out Actor outValue)
        {
            if (blackboardActorData.ContainsKey(key))
            {
                outValue = blackboardActorData[key];
                return true;
            }
            outValue = null;
            return false;
        }

        public Actor GetValue(string key)
        {
            Actor value;
            GetValue(key, out value);
            return value;
        }
    }*/
}
